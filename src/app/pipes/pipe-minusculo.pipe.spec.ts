import { PipeMinusculoPipe } from './pipe-minusculo.pipe';

describe('PipeMinusculoPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeMinusculoPipe();
    expect(pipe).toBeTruthy();
  });
});
