import { AlterarCorDirective } from './alterar-cor.directive';

describe('AlterarCorDirective', () => {
  it('should create an instance', () => {
    const directive = new AlterarCorDirective();
    expect(directive).toBeTruthy();
  });
});
